//open a function
(function ($) {
 
 	//open when document is ready
	$(document).ready(function () {
		//for the accordions to open and close the right way
		$accordion = $("#accordiongs");
			$accordion.accordion({
				collapsible: true,
				heightStyle: "content",
				active: false
			});

	//stop document ready function
	});

//stop function at the top
})(jQuery);